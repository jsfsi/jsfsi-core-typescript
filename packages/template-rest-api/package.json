{
  "name": "@jsfsi-core/template-rest-api",
  "version": "2.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "scripts": {
    "build": "npm run lint && rm -rf dist && tsc && cp -r configuration dist/configuration",
    "clean": "rm -rf dist && rm -rf node_modules && rm -rf coverage",
    "db:migrations:generate": "npm run build && export $(cat ./configuration/.env | xargs) && typeorm migration:generate -d ./dist/src/repositories/MigrationsDataSource.js",
    "db:migrations:run": "npm run build && typeorm migration:run -d ./dist/src/repositories/MigrationsDataSource.js -t each",
    "db:migrations:run:local": "export $(cat ./configuration/.env | xargs) && npm run db:migrations:run",
    "dev": "rm -rf dist && tsc && cp -r configuration dist/configuration && NODE_ENV=development concurrently \"tsc --watch --preserveWatchOutput\" \"node --watch-path=dist --enable-source-maps --experimental-print-required-tla dist/src/main.js\"",
    "docker:build": "docker build -t core-api -f Dockerfile ../..",
    "format": "biome format --write src",
    "lint": "biome check --write src",
    "lint:fix": "biome check --write src",
    "start": "node --enable-source-maps dist/src/main.js",
    "start:debug": "rm -rf dist && tsc && cp -r configuration dist/configuration && NODE_ENV=development concurrently \"tsc --watch --preserveWatchOutput\" \"node --watch-path=dist --inspect --enable-source-maps --experimental-print-required-tla dist/src/main.js\"",
    "start:dist": "node dist/src/main.js",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:integration": "vitest run --config ./vitest.integration.config.mts",
    "test:integration:watch": "vitest --config ./vitest.integration.config.mts",
    "test:unit": "vitest run --config ./vitest.unit.config.mts",
    "test:unit:watch": "vitest --config ./vitest.unit.config.mts",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@google/genai": "2.19.0",
    "@jsfsi-core/ts-crossplatform": "file:../ts-crossplatform",
    "@jsfsi-core/ts-nestjs": "file:../ts-nestjs",
    "@jsfsi-core/ts-nodejs": "file:../ts-nodejs",
    "@nestjs/axios": "4.0.1",
    "@nestjs/cli": "11.0.24",
    "@nestjs/common": "12.0.1",
    "@nestjs/config": "12.0.0",
    "@nestjs/core": "12.0.1",
    "@nestjs/platform-express": "12.0.1",
    "@nestjs/typeorm": "12.0.0",
    "axios": "1.20.0",
    "body-parser": "2.3.0",
    "express": "5.2.1",
    "firebase-admin": "14.3.0",
    "pg": "8.23.0",
    "reflect-metadata": "0.2.2",
    "typeorm": "1.1.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@nestjs/testing": "12.0.1",
    "@swc/core": "1.16.1",
    "@types/supertest": "7.2.1",
    "@vitest/coverage-v8": "4.1.11",
    "supertest": "7.2.2",
    "ts-node": "10.9.2",
    "unplugin-swc": "1.5.11",
    "vite": "8.2.2",
    "vitest": "4.1.11"
  }
}
